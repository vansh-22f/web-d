promise methods-> promise.all like this left

objects methods


e.stopPropagation();           // stops bubbling
e.stopImmediatePropagation();  // stops all listeners

form.addEventListener("submit", e => {
  e.preventDefault();
});
